#===============================================================================

# Each of these calls will add one library plus multiple apps and unit tests.
# - The arguments are: (library_name, [list of folders needed for library])

add_isis_module(base base database control qisis)
